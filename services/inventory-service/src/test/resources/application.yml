spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  # ── Database Migration (Flyway) ─────────────────────────────
  flyway:
    baseline-on-migrate: false
    enabled: true
    locations:
      - classpath:db/migration
